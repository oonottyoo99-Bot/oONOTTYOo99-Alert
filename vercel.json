# 1. แก้ไฟล์ vercel.json
cat > vercel.json <<EOF
{
  "version": 2,
  "functions": {
    "api/index.py": {
      "runtime": "vercel-python@0.5.2"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.py" }
  ]
}
EOF

# 2. add + commit
git add vercel.json
git commit -m "Fix: update vercel runtime for python"

# 3. push ขึ้น GitHub
git push origin main

# 4. กลับไปหน้า Vercel -> กด Redeploy
